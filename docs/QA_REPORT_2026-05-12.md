# Pulse CRM QA Report - 2026-05-12

## Scope

Validated the deployed Pulse CRM test environment at `https://pulse-crm.theclustox.com` and the backend regression suites for the modules built so far: auth/admin, leads, territory, training, consignment, product management, digital assets/Widen replacement, dealer portal, calendar, customers/accounts, and catalog-rule administration.

## Fixes From This QA Pass

- Added and deployed product catalog publish compare before snapshot publish: current draft vs published version counts, added/removed/changed products, unchanged count, and warnings.
- Stabilized lead readiness regression data so duplicate protection is tested intentionally without blocking unrelated conversion fixtures.
- Stabilized training ops/compliance regression data so expiring certification checks remain valid after calendar time moves forward.
- Repaired the production browser smoke harness to authenticate through the same token storage path the app uses.

## Automated Results

| Area | Evidence | Result |
| --- | --- | --- |
| Product Management + Digital Assets | `pnpm --filter @pulse/api exec node --test --test-concurrency=1 test/product-management.assets.regression.test.mjs` | 13 passed / 0 failed |
| Dealer Portal | `pnpm --filter @pulse/api test:dealer-portal` | 7 passed / 0 failed |
| Leads | `pnpm --filter @pulse/api test:leads` | 71 passed / 0 failed |
| Territory | `pnpm --filter @pulse/api test:territories` | 28 passed / 0 failed |
| Training | `pnpm --filter @pulse/api test:training` | 20 passed / 0 failed |
| Consignment | `pnpm --filter @pulse/api test:consignment` | 14 passed / 0 failed |
| Auth/Admin | `pnpm --filter @pulse/api test:admin` | 19 passed / 0 failed |
| Production UI smoke | `output/playwright/pulse-qa-2026-05-12/qa-browser-smoke.mjs` | 15 passed / 0 warnings / 0 failed |

## Production UI Smoke Coverage

The Playwright smoke run opened the deployed app with a super-admin session and validated visible loading for: home, lead pipeline, website forms, finance queue, territory workspace, territory map, training, consignment, account/customer area, calendar, digital assets, product management, product readiness, admin users, and admin catalog rules.

Screenshots and JSON evidence are under `output/playwright/pulse-qa-2026-05-12/`.

## Residual Gaps / Parked Dependencies

- Acumatica-backed product truth, consignment warehouse truth, and final product CSV migration remain parked until integration/data mapping is confirmed.
- Real Microsoft Graph notification delivery is covered by provider-contract tests and disabled/missing-config behavior, but live tenant delivery still needs production credentials and recipient approval.
- Dealer portal UI self-service flow is backend-covered; full browser automation needs a seeded dealer user journey separate from the admin smoke session.
- Asset binary upload to real S3/CloudFront should get a final live-file acceptance pass once production bucket policies and sample files are approved.

## Deployment Evidence

Latest deployed release from this work: `manual-20260512225107-catalog-compare`.

Latest source commits:

- `Stabilize QA regression evidence`
- `402595a Add dealer catalog publish compare`
- `2897c43 Add dealer catalog snapshots`
